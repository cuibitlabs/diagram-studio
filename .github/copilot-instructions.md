# Diagram Studio instructions

When asked to work on a diagram or its import/export path, read `skills/create-editorial-diagrams/SKILL.md` and the relevant linked reference. Keep SVG accessible, project JSON versioned, and the browser and Python importers compatible. Validate changes with `npm run check` and `python3 test/test_skill_tool.py`.
